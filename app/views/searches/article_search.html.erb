<h1>articles search for '<%= params[:search_word] %>' </h1>
		<table class= "table table-hover table-inverse"　>
			<thead>
				<tr>
					<th></th>
					<th>Title</th>
					<th>Opinion</th>
					<th colspan="3"></th>
				</tr>
			</thead>
		<tbody>
			<% @articles.each do |article|%>
				<tr>
	      			<td><%#= attachment_image_tag book.user, :profile_image, :fill, 60, 60, format: 'jpeg', class: "img pull-left profile-img", fallback: "no_image.jpg" %></td>
         			<td><%= article.title %></td>
         			<td><%= article.body %></td>
         			</tr>
      		<% end %>

		</tbody>
		</table>